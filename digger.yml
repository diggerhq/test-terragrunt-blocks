
generate_projects:
  blocks:
    - block_name: dev
      terragrunt: true
      rootDir: "projects/test1"
      workflow: default
    - block_name: staging
      terragrunt: true
      rootDir: "projects/test2"
      workflow: default
    - block_name: dev
      terragrunt: true
      rootDir: "projects/test3"
      workflow: default

workflows:
  default:
    plan:
      steps:
        - init:
          extra_args: ["-upgrade"]
        - plan
