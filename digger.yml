generate_projects:
  blocks:
    - block_name: dev
      terragrunt: true
      root_dir: projects/test1
      workflow: default
      workflow_file: workflow_dev.yml

workflows:
  default:
    plan:
      steps:
        - init:
          extra_args: ["-upgrade"]
        - plan
